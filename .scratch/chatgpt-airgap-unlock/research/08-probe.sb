(version 1)

; Obsolete after ticket 08 split the shared profile into role-specific files.
; Retained only for the ticket 13 evidence reference; no ticket 08 caller may
; use it, and any accidental use fails closed.
(deny default)
